import { colors } from '@/theme';

export const routeLine = {
  strokeColor: colors.brand,
  strokeWidth: 8,
} as const;

export const stopDot = {
  size: 12,
  fill: colors.white,
  border: colors.brand,
  borderWidth: 2,
} as const;
